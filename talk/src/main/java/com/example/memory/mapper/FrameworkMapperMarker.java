package com.example.memory.mapper;

import org.apache.ibatis.annotations.Mapper;

/**
 * Placeholder mapper to keep MyBatis scanning in place before real mapper interfaces are added.
 */
@Mapper
public interface FrameworkMapperMarker {
}
